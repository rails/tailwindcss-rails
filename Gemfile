source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }
gemspec

gem "debug", ">= 1.0.0"

group :test do
  gem "actionmailer", ">= 7.0.0"
  gem "minitest-mock"
end
